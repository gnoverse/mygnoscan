# Binaries
*.exe
*.dll
*.so
*.dylib

# Test binary
*.test

# Output of go coverage
*.out

# Go workspace file
go.work
go.work.sum

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
mygnoscan
mygnoscan.db

# local dev server config (machine-specific paths)
.claude/launch.json

# Per-machine agent settings. Not shared: the contents are one developer's
# allow-list, naming their own paths and their own hosts, and a repo is the
# wrong place to keep either.
.claude/settings.local.json

# browser tests: dependencies and per-run output. e2e/package-lock.json is
# committed, everything else here is generated.
e2e/node_modules/
e2e/.tmp/
e2e/test-results/
e2e/playwright-report/
docs/images/review/
